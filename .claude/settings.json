{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // empty'); if printf '%s' \"$cmd\" | grep -qE 'shopify[[:space:]]+theme' && printf '%s' \"$cmd\" | grep -qE 'push|publish|delete|dev' && printf '%s' \"$cmd\" | grep -q '166528450774'; then echo 'BLOCKED: 166528450774 is the live MAIN theme (Copy of Modus Final). Never push/publish/delete/dev here.' >&2; exit 2; fi; exit 0"
          }
        ]
      }
    ]
  }
}
